# Work Packages

*Progress: [25 / 36 tasks complete] — approximately 69% complete*

-----

## Phase 1: Discovery & Planning

### WP1.1 - Reference Site Analysis

- [x] @claude - Extract atlascoffeeclub.com structure, subscription model, and features
- [x] @user - Review PM docs and confirm scope/direction

### WP1.2 - Business Inputs

- [x] @claude - Define placeholder catalog + pricing (Atlas structure/pricing +15%, see Technical Reference)
- [x] @claude - Populate placeholder catalog into Shopify store (6 products, 3 collections, all Draft status — see Technical Reference)
- [x] @user - Confirm placeholder catalog/pricing → all 6 products flipped to Active status (was Draft). Note: Shopify Payments still not enabled (WP2.2), so checkout can't actually process payment yet even though products are live/purchasable-looking. Storefront password-protection status unconfirmed (Admin-UI only, not checkable via API) — check Online Store → Preferences to know if this is publicly reachable yet.
- [x] @user - Confirm Shopify store status and provide access → existing store connected via Shopify MCP connector (Brown Bag Coffee Club, Basic plan)

### WP1.3 - Payments

- [x] @user - Clarify Square usage → decided: use Shopify Payments for this site (comparable/cheaper rate, no gateway conflict)

### WP1.4 - Brand Architecture

- [x] @user - Clarify company vs. subscription-brand naming → High Ground Coffee (parent) / Brown Bag Coffee Club (subscription brand, storefront name)

-----

## Phase 2: Shopify Build

### WP2.1 - Storefront Theme

- [x] @claude - Vendor Dawn (Shopify OS 2.0 reference theme) into repo at `theme/` — *superseded: user chose Vessel instead; Dawn vendor removed from repo*
- [x] @user - Choose theme → Vessel (paid Theme Store theme); user installing/connecting it directly in Admin
- [x] @user - Provide Vessel's actual theme code (exported .zip from Admin → Edit code → Download) after Theme Access token and GitHub-connect both hit dead ends — now in repo at root
- [x] @claude - Build subscription plan builder section (roast profile + quantity + brew method) → **not needed as custom work**: confirmed Vessel's native `variant-picker`, `quantity`, and `buy-buttons` blocks already handle this once product options are set correctly (which they are)
- [x] @claude - Rebrand homepage (`templates/index.json`): hero copy, real collection references (coffee-subscriptions, gifts), removed empty demo sections
- [x] @claude - Wire up main navigation + footer menus to real collections/pages (via Shopify MCP connector's `menuUpdate` GraphQL mutation — see Technical Reference)
- [x] @user - Get this repo's theme code live on the store → **resolved**: Vessel's per-theme "Connect to GitHub" now connected to `main`; earlier failure was just `main` missing the theme files, not a broken integration. Live sync confirmed working.
- [x] @claude - Add placeholder imagery to hero, products, and collections (all had zero images) — free-license stock photography via Shopify MCP; flagged in Technical Reference as placeholder, not final brand photography
- [x] @claude - Set SEO title/description on all 6 products and 3 real collections (none had any set) — see Technical Reference
- [ ] @claude - Set SEO metafields (`global.title_tag`/`description_tag`) on content pages once they're published (skipped for now — pages are still unpublished drafts pending legal review/real contact info)
- [x] @claude - Shipping/fulfillment audit: fixed 0 lb weight on all 40 variants (would've broken international carrier-calculated rates), confirmed inventory-tracking-off makes the DENY policy moot, confirmed shipping zones/rates already configured correctly — see Technical Reference
- [x] @user - Purchase and connect custom domain → `brownbagcoffeeclub.com` (+ www/account subdomains), SSL enabled, confirmed set as primary domain via API. Domain purchase/connection/primary-switching has no Admin API path at all — done manually in Admin → Settings → Domains.

### WP2.2 - Subscriptions & Commerce

- [x] @user - Install a subscription app → switched from Skio to Shopify's free first-party **Shopify Subscriptions** app (Skio's widget wouldn't render reliably even placed correctly). Installed and configured: "Subscribe and Save" plan, monthly billing, 10% off, attached to all 5 coffee-format products (Core, Decaf, Pods, Espresso Pods, Bulk). Gift Subscription correctly excluded (one-time gift item). An initial yearly tier was tried and removed — Shopify's native app couples billing/delivery interval (can't do "bill yearly, deliver monthly" like a dedicated subscription app), so keeping it simple with monthly only for now.
- [ ] @user - Enable Shopify Payments on the store — requires real banking/tax info; no path for Claude to supply this
- [x] @claude - Integrate subscription app into theme/checkout flow — widget added to `templates/product.json` between variant picker and buy buttons (single instance, after cleaning up two duplicate/misplaced instances along the way)
- [ ] @user - Confirm the subscription widget renders full-width/centered correctly on the live product page — Claude can't render the page to verify visually; check Theme Editor block settings for a layout/width option if it looks off

### WP2.3 - Content Pages

- [x] @claude - Build About, How It Works, Our Coffee, Brew Guides, FAQ, Contact Us as Shopify Pages (unpublished, see Technical Reference)
- [x] @claude - Draft legal pages (Terms, Privacy, Accessibility, Cookie Policy) as unpublished placeholders, clearly flagged as not-legal-advice
- [x] @user - Have Terms & Conditions, Privacy Policy, Cookie Policy reviewed by an attorney before publishing → **attorney review came back positive**
- [ ] @user - Fill in real contact details, shipping policy specifics, and business jurisdiction before publishing (marked as placeholders in the page content: About Us, Our Coffee, Brew Guides, Accessibility Statement, FAQ, Contact Us, legal pages)
- [ ] @user - Paste placeholder policy text into Settings → Policies (Refund, Privacy, Terms, Shipping, Subscription) — Shopify MCP connector lacks `write_legal_policies` scope, so this can't be done via Claude; checkout currently shows Shopify's raw unfilled default template with broken merge tags. Exact text provided in chat/Session Log. Status as of session end: unconfirmed whether this got done.
- [ ] @user - Decide on the orphaned empty "Contact" demo page (handle `contact`, not `contact-us`) — leftover from the original Vessel install, blank body, published but not linked anywhere. Delete or leave alone?

### WP2.4 - Marketing & Google Ads

- [x] @claude - Draft full Google Ads Search campaign content: campaign settings, 4 ad groups (Core, Decaf, Gift, Pods), keyword lists, negative keywords, RSA headlines/descriptions for the core ad group, sitelinks, callouts, structured snippets — see Session Log for full text
- [ ] @claude - Write remaining ad group copy (headlines/descriptions) for Decaf, Gift, and Pods ad groups — core group done, others need adaptation
- [ ] @user - Actually create the campaign in Google Ads (no Google Ads connector available in this session — content is ready to paste in)
- [ ] @claude - Set up conversion tracking before spending real ad budget — via Shopify's native Google & YouTube sales channel (recommended) or a manual gtag snippet in `layout/theme.liquid`
- [ ] @user - Don't launch real ad spend until Shopify Payments is enabled and checkout is confirmed working end-to-end
- [ ] @user - Phase 2 (later): connect Google Merchant Center for Performance Max/Shopping campaigns, once the Search campaign is validated
